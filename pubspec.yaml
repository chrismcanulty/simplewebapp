name: muse_operation_ui
description: FlutterKaigi 2022「アプリの安全な変化と拡大を支えるアーキテクチャと単体テスト」のためのサンプルアプリ
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.18.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  freezed_annotation: ^2.1.0
  flutter_hooks: ^0.18.0
  hooks_riverpod: ^1.0.4
  retrofit: ^3.0.1+1
  dio: ^4.0.6
  json_annotation: ^4.7.0
  shared_preferences: ^2.0.15
  sprintf: ^7.0.0
  flutter_markdown: ^0.6.12
  url_launcher: ^6.1.5
  flutter_launcher_icons: ^0.9.2
  responsive_framework: ^0.2.0
  mqtt_client: ^10.2.1
  web_socket_channel: ^2.2.1
  helpers: ^1.2.0
  modal_bottom_sheet: ^3.0.0-pre
  amplify_api: ^2.0.0
  amplify_auth_cognito: ^2.0.0
  amplify_authenticator: ^2.0.0
  amplify_flutter: ^2.0.0
  go_router: ^6.5.5
  sigv4: ^5.0.0
  amazon_cognito_identity_dart_2: ^3.3.0
  aws_dynamodb_api: ^2.0.0
  http: ^0.13.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.2.1
  freezed: ^2.1.0+1
  retrofit_generator: ^4.0.3+2
  json_serializable: ^6.4.0
  mockito: ^5.3.2

flutter:
  uses-material-design: true
